{
  "name": "@projectx/hospitality-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Hospitality Management Compose Server — properties, rooms, reservations, folios, housekeeping, services, partners, venues",
  "exports": {
    ".": "./src/index.ts",
    "./db/schema/hospitality": "./src/db/schema/hospitality.ts",
    "./db/seed/hospitality": "./src/db/seed/hospitality.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "elysia": "^1.4.25"
  },
  "devDependencies": {
    "@projectx/config": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.3.3"
  }
}
